.DS_Store
.idea/

# Node
node_modules/
dist/
*.tsbuildinfo

# Expo (legacy prototype)
RaceMind/.expo/

# Python
ml/.venv/
__pycache__/

# Raw OpenF1 cache: ~55MB, fully reproducible via `python ingest.py`
ml/raw/
ml/out/*.parquet

# The legacy Expo prototype used to live here as a nested git repo, which made the
# editor show two unrelated repositories side by side. It now sits outside this
# project at ../RaceMind-legacy with its own history intact. Kept ignored in case a
# stray copy reappears.
RaceMind/
